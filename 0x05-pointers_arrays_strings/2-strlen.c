#include "main.h"

/*
 * _strlen - return the length of a string
 * @s: string to check
 * Return: the length of a string in int
 */

int_strlen(char *)
{
	int a = 0;

	while (s[a] != '\0')
	{
		a++;
		*s++;
	}
	return (a);
}
