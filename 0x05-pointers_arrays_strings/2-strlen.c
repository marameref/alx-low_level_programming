#include "main.h"
#include <stdio.h>

/*
 * _strlen - return the length of a string
 * @s: string to check
 * Return: the length of a string in int
 */
int_strlen(char *str)
{
	int length = 0;

	while (s[length] != '\0')
	{
		length++;
	}

	return (length);
}
